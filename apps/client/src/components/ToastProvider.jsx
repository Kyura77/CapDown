/* eslint-disable react-refresh/only-export-components */
// Legacy re-export — consolidated into Toast.jsx
// Kept so old import paths (App.jsx etc.) don't break
export { ToastProvider, useToast } from './Toast';
